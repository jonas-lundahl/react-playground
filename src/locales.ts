import { enUS as core_enUS } from "@mui/material/locale";
import { enUS as picker_enUS } from "@mui/x-date-pickers/locales";

const coreLocale = core_enUS;
const pickerLocale = picker_enUS;

export { coreLocale, pickerLocale };
